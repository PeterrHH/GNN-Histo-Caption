dataset_path: "../../../../../../srv/scratch/bic/peter/Report"
histocartography_path: "../histocartography/histocartography"
graph_path: "../../../../../../srv/scratch/bic/peter/full-graph"
encoder_path: "../../../../../../srv/scratch/bic/peter/encoder"
decoder_path: "../../../../../../srv/scratch/bic/peter/decoder"
output_path: "output"
vocab_path: "new_vocab_bladderreport.pkl"
learning_rate: 0.001
weight_decay: 0.005
phase: train # train, test, eval
epochs: 100
decoder_type: "LSTM" # Transformer, LSTM
train_mode: "train_mode"
encoder: "CNN"
graph_model_type: "Hierarchical" #  Hierarchical, Cell, Tissue
optimizer_type: "Adam" # Adam, SGD
batch_size: 64
eval_batch_size: "full"
model_save_base_path: "../../../../../../srv/scratch/bic/peter/model_save"
save_model: True
save_encoder: "Encoder6" # put encoder name
save_decoder: "Decoder6" # put decoder name 
save_global_feature_extractor: "GlobalExtractor6" # put feature extractor name
save_classifier: "Classifier1" #  
save_every: 2
loss: label # Both, caption, label
wandb_name: "BS64 lr 0.001 wd 0.005 Adam all 5 cap #6 LSTM ONLY CNN"
load_encoder_name : "Encoder3-14-2.46.pt"
load_global_extractor_name: "GlobalExtractor3-14-2.46.pt"
load_decoder: "Decoder3-14-2.46.pt"
load_train: False
classifier_name: "lr lr=0.0001 #3 lowest"


gnn_param:
  cell_layers: 3
  tissue_layers: 3
  cell_conv_method: "GraphSage" # GCN, GAT, GraphSage, GIN, PNA
  tissue_conv_method: "GraphSage" # GCN, GAT, GraphSage, GIN, PNA
  pool_method: None # None, Diff, MinCut
  aggregate_method: "mean" # sum, mean, max
  hidden_size: 256
  output_size: 256
  GAT: 
    num_heads: 2
  GraphSage:
    aggregator_type: "gcn" # mean, gcn, pool, lstm
  GIN:

global_class_param:
  hidden_size: 512
  output_size: 256
  dropout_rate: 0.3

classifier_param:
  num_class: 3
  hidden_size: 64
  dropout_rate: 0.5

transformer_param:
  n_head : 4
  num_layers: 3
  dim_feedforward: 2048
  dropout: 0.2


lstm_param:
  dropout: 0.3 # too much dropout in lstm breaks it
  num_layers: 1
  size: 512
  bi_direction: False

